# Only used by --provider openai. Fixture mode is offline and needs no key.
OPENAI_API_KEY=
# Choose a model in your account with structured outputs and image inputs.
# The harness deliberately has no implicit paid-model default.
OPENAI_MODEL=
